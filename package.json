{
  "name": "tapntrust-storefront",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "check": "node scripts/check-imports.mjs && node scripts/check-cart-integrity.mjs && node scripts/check-welcome-offer.mjs && node scripts/check-clarity-events.mjs && node scripts/check-invariants.mjs && wrangler types --check -c insights-worker/wrangler.jsonc insights-worker/worker-configuration.d.ts && tsc -p insights-worker/tsconfig.json --noEmit && vitest run --config insights-worker/vitest.config.ts",
    "check:insights": "wrangler types --check -c insights-worker/wrangler.jsonc insights-worker/worker-configuration.d.ts && tsc -p insights-worker/tsconfig.json --noEmit && vitest run --config insights-worker/vitest.config.ts",
    "dev:insights": "wrangler dev -c insights-worker/wrangler.jsonc"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.6",
    "@types/node": "^22.20.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.130.0"
  }
}
